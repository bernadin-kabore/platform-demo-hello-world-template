# ${{ values.name }}

${{ values.description }}

Generated by the platform's `hello-world-go` Backstage template. See
`docs/index.md` for the full picture.

**First-time setup**: run `go mod tidy` once locally and commit the
resulting `go.sum` — the CI build requires one and it isn't generated by
the scaffolder.
