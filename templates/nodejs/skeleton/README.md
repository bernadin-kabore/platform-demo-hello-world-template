# ${{ values.name }}

${{ values.description }}

Generated by the platform's `hello-world-nodejs` Backstage template. See
`docs/index.md` for the full picture — this file is intentionally short so
`docs/index.md` (rendered by TechDocs) stays the source of truth.

**First-time setup**: run `npm install` once locally and commit the
resulting `package-lock.json` — `npm ci` in CI requires one and it isn't
generated by the scaffolder.
